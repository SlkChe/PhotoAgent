# История решений проекта

## Организация дизайн-работ — 2026-09-22

- По указанию владельца новые дизайн-артефакты размещаются в `docs/ui/`,
  рассуждения, открытые вопросы и инструкции frontend — в `docs/mvp-1/`.
- Подготовлен `docs/mvp-1/ui-design-questions.md` для переработки UI-концепции
  MVP-1. Содержащиеся в нём варианты поведения ещё не являются принятыми решениями.

## Уточнения интерфейса MVP-1 — 2026-09-22

| Решение владельца | Область применения |
|---|---|
| Аудитория — начинающий и развивающийся фотограф: качество снимков, насмотренность и эрудиция | UX/UI MVP-1 |
| Пользователь анонимный; backend автоматически выбирает роль ассистента по каждой реплике и контексту, включая обращение к роли без вопроса | Маршрутизация и UI |
| Пять тематик влияют на выбор роли; уточнение может переключить специалиста без нового диалога | Маршрутизация |
| Начальное приглашение «О чём поговорим сегодня?» без имени; последующие ответы имеют имя ассистента до 20 символов. Имена и образы сейчас не проектируются, иконки в MVP-1 отсутствуют; будущий предел иконки — 32 × 32 px | Представление ассистента |
| Включить предложенного владельцем ассистента-друга как роль по умолчанию и для очень широких вопросов | Концепция; детали маршрутизации требуют проработки |
| На правых полях отражаются ссылки всего текущего диалога | Источники |

Новая концепция: `docs/ui/concept.md`; схема: `docs/ui/layout.svg`;
передача специалистам: `docs/mvp-1/ui-handoff.md`. Визуальное направление и
дополнительные механики предложены на согласование. Код приложения не изменён.

## Утверждение UI-концепции с дополнениями — 2026-09-22

| Решение | Область применения |
|---|---|
| Владелец утвердил концепцию и схему компоновки | `docs/ui/`, MVP-1 |
| Поле ввода высотой 4–7 строк; рост по содержимому, затем внутренняя прокрутка | Ввод сообщения |
| Карточка содержит контекстный комментарий; меню «Перейти к диалогу» прокручивает к первичному ответу; «Открыть ссылку» и клик по ссылке открывают новую вкладку | Источники |
| Повтор точного URL не создаёт карточку; первичная привязка сохраняется. Разные URL автоматически не объединяются | Источники |
| Сохранение диалога в .md и всех источников в отдельный файл включено в MVP-1; для источников дизайнер выбрал .md | Пользовательский экспорт, без серверной постоянной истории |

Детальные технические ответы ещё не получены; вопросы актуализированы в
`docs/mvp-1/ui-handoff.md`. Утверждение концепции не означает реализацию или закрытие U-01–U-04.

## Объединение уточнений специалистам — 2026-09-22

Рекомендации из `docs/mvp-1/ui-handoff.md` объединены с существующими разделами
`docs/mvp-1/decisions.md`; восстановление роли, первичные связи источников,
экспорт и проверки данных дополнены в `docs/mvp-1/session-lifecycle.md`.
Повторный список вопросов специалистам в UI-брифе заменён ссылкой на общий список.
`ui-handoff.md` сохранён только как указатель. Открытые технические вопросы
не объявлены решёнными; структура разделов замечаний специалистам сохранена.

## Дизайн U-01/U-02 и очистка старых артефактов — 2026-09-22

- Владелец подтвердил: объединяются только полностью совпадающие URL.
- По разрешению владельца старые `docs/ui-concept.md`, три `docs/chat-*.png`
  и указатель `docs/mvp-1/ui-handoff.md` перенесены в корзину macOS,
  подкаталог `PhotoAgent-design-20260922`. Упоминания выше описывают прошлые этапы;
  актуальные ссылки обновлены, отдельный handoff больше не поддерживается.
- U-01: подготовлены потоки, матрица состояний, покрытие US-01–US-12 и предложения
  языка/экранов/доступности в `docs/mvp-1/ui-flows.md`.
- U-02: подготовлен `docs/ui/chat-prototype.html` с 22 состояниями и снимками
  первого экрана/разговора для 1440 и 390 px. Макеты не подключены к API.
- В Chrome проверены четыре размера (1440, 1024, 390, 320 px), отсутствие
  горизонтального переполнения, блокировка и рост ввода, переход к первичному
  ответу, демонстрационный экспорт и подтверждение очистки. Ошибок JS нет.
- Создание макетов не заблокировано открытыми вопросами. Финальная приёмка U-02
  зависит от A-11; проверка реальных устройств, Safari и доступности ещё предстоит.

## Среды UI и закрытие U-01 — 2026-09-22

- Владелец согласовал поддержку Chromium, WebKit и Gecko; основные проверки —
  Google Chrome и Apple Safari на текущем Mac. Проверку Firefox при необходимости
  запрашиваем у владельца для выполнения на другом ПК.
- Приоритет: desktop HD/Full HD/4K landscape; мобильный веб Android —
  360 × 800 и 390 × 844 portrait. Отдельное мобильное приложение не требуется.
  Разрешение дисплея и viewport в CSS px различаются в протоколе проверки.
- U-01 закрыта: потоки, матрица и параметры сред зафиксированы в ui-flows.md;
  доступность предложена согласно критериям задачи. A-11 целиком не закрывается.
- По предложению владельца макет дополнен иконками меню и источников; дизайнер
  предлагает открытие одной панели поверх диалога с явным закрытием. Свайп
  отложен как необязательное дополнение. Это детализация, а не отчёт о готовности UI.
- Chrome: проверены viewport 1280 × 720, 1920 × 1080, 3840 × 2160, 360 × 800,
  390 × 844, отсутствие горизонтального переполнения на экране истории,
  переключение панелей, Escape, сохранение черновика и переход к первичному ответу.
  Safari, Firefox и реальный Android в этом проходе не проверялись.
- Комплект U-01/U-02 доступен для начала frontend и подготовки тестов. Следующие
  работы: U-03, согласование контрактов, реализация и проверка U-04/F-06/T-05.

## Задачи реализации UI и проверка Safari — 2026-09-22

- По предложению владельца введены статусы после ID: `(+)` закрыта, `(!)` в работе,
  без отметки — не закрыта и не в работе. U-01 отмечена (+), A-01–A-05 указаны
  как ранее закрытые со ссылками; текущих задач (!) нет.
- F/T дополнены ролями, точной дедупликацией URL, контекстом и первичной привязкой,
  вводом 4–7 строк, адаптивностью, доступностью и экспортом. Добавлены F-07/F-08
  и T-07/T-08, порядок и зависимости актуализированы без смены прежних ID.
- Для устранения незакреплённой ответственности добавлены A-13 (согласование
  контрактов UI) и B-13 (снимок экспорта), расширены зависимые backend-задачи.
  Порядок согласования и необходимые ответы записаны в docs/mvp-1/decisions.md.
- safaridriver доступен, но Safari не разрешил создать сессию: отключено
  Allow remote automation. Настройки не менялись; успешная проверка Safari
  не заявляется. Владелец может включить опцию либо проверить макет вручную.

## Экспорт и завершение дизайн-сессии — 2026-09-22

- Владелец подтвердил: явное скачивание диалога/источников не продлевает TTL.
  Правило и проверки внесены в session-lifecycle.md; открытый продуктовый вопрос
  удалён из замечаний специалистам, требования и A-13/B-13 актуализированы.
- Технические согласования A-13/A-07 и другие открытые задачи остаются у владельцев
  соответствующих компонентов. Независимые работы команды можно начинать;
  готовность интеграции и пилота пока не утверждается.
- По уточнению владельца docs/mvp-1/tasks.md используется только локально,
  не включается в Git; существующее правило .gitignore сохраняется без изменений.
- По поручению владельца результаты подготовлены к локальному Git-коммиту,
  без push. Дальнейшая работа — после ответов и вопросов участников команды.

## Передача контекста следующей сессии — 2026-09-22

- Концепция утверждена: docs/ui/concept.md; потоки и закрытие U-01:
  docs/mvp-1/ui-flows.md; интерактивные макеты U-02 и результаты проверок:
  docs/ui/README.md и docs/ui/chat-prototype.html.
- Следующий шаг дизайнера — U-03 (ответ, доказательства, источники, обратная связь),
  затем U-04 (проверка реализации). Сейчас ждём ответов и вопросов команды;
  без нового поручения дальнейшая разработка не запускается.
- Открытые технические согласования и ответственные: docs/mvp-1/decisions.md,
  раздел A-13; требования сессии/экспорта: docs/mvp-1/session-lifecycle.md.
  Скачивание не продлевает TTL. Независимые работы команды не заблокированы,
  но готовность интеграции и пилота не подтверждена.
- Chrome проверен в объёме, указанном в docs/ui/README.md. Safari не проверен:
  отключено Allow remote automation. Владелец может включить опцию или проверить
  вручную. Firefox по необходимости проверяет владелец на другом ПК.
- tasks.md — исключительно локальный рабочий список, не коммитить и не менять
  ради него .gitignore. Файл сохраняется на этом компьютере; в новом checkout
  его не будет. Ссылки на него в документации предназначены для локальной работы.
- Локальный коммит исправляется без включения tasks.md; push не выполнялся.
  Эта запись сохраняет рабочие решения и следующие шаги, а не дословную переписку.

## Текущий статус проекта

Реализован прототип 0.1: FastAPI-сессии в памяти, каркас декомпозиции и четыре
обработчика-заглушки; Streamlit-чат с концептом оформления и управлением сессией.
Реальных вызовов ИИ, поиска, загрузки файлов и постоянного хранения истории нет.

## Принятые решения — 2026-09-11

| Решение | Область применения |
|---|---|
| Использовать `snake_case` для Python-модулей и каталогов импортируемых пакетов; `kebab-case` для каталогов сервисов и документов. Сохранять стандартные имена файлов инструментов. | Весь репозиторий |
| Запускать тесты через `python -m unittest discover` из корня репозитория с относительными путями и шаблоном `*_test.py`. | Тесты |
| Документировать весь HTTP API в OpenAPI: запросы, ответы, предусмотренные ошибки и фактические форматы данных. | Бэкенд |
| Кешировать в UI только допустимые повторяемые запросы чтения с TTL и ограничением размера. В первой версии ограничиться справочниками и общедоступным поиском. Историю и ответы хранить в состоянии сессии. | Streamlit UI |
| Не применять кеширование в первых версиях `core-api`. | Бэкенд |
| Передавать секреты через окружение при запуске и читать через отдельный `BaseSettings` каждого сервиса. Не хранить секреты в файлах. Для локальной отладки использовать скрытый ввод; `.env` допускается только для несекретных настроек. `st.secrets` не использовать. | Конфигурация сервисов |
| Применять профильные инструкции по затрагиваемой области независимо от роли агента. | Работа над проектом |
| «Утвердительный ответ» означает подтверждение предыдущего предложения: «да», «продолжай». | Декомпозиция запросов |
| На первом шаге использовать заменяемый классификатор по правилам. Допускать несколько тем в реплике; обработчики возвращают только названия тем. | Бэкенд 0.1 |
| Хранить контекст в памяти одного процесса API; UI хранит только копию текущего диалога для отрисовки, без восстановления после нового открытия. | Сессии 0.1 |
| Удалять сессию по «Новому чату»; при закрытии страницы пытаться отправить браузерный сигнал. Резервная очистка: TTL 180 секунд без heartbeat и проход очистки каждые 10 секунд. | Жизненный цикл сессий |
| Повторную отправку одной реплики определять по request_id; это защита от дубликатов в контексте, не кеширование запросов. | HTTP-контракт |
| TTL каждой сессии брать из `PHOTO_API_SESSION_TTL_SECONDS` (по умолчанию 180 секунд). Это скользящий TTL без активности: реплика или heartbeat продлевают `expires_at`. Предельная длительность активного разговора не устанавливается. | Настройки сессий |
| Ограничить прототип локальным запуском без авторизации: один worker API, до 1000 сессий, до 100 пар реплик на сессию и до 4000 символов в запросе. Лимиты сессий и реплик задаются окружением. | Границы прототипа |

## Проверки прототипа

- Python 3.14.7: 18 тестов `unittest` для API, хранилища сессий и Streamlit UI.
- `ruff check .`, `ruff format --check .`, `git diff --check` прошли без ошибок;
  `pip check` не обнаружил конфликтов зависимостей.
- Проверены в Chrome: реплики, «Новый чат», мобильная вёрстка и удаление при уходе со страницы.
- Команды локального запуска и отладки описаны в `README.md` и `AGENTS.md`.
- Конфигурация Ruff и Docker Compose добавлена. Docker отсутствует в текущем окружении;
  сборка и запуск контейнеров ещё не проверены.

## Завершение работы — 2026-09-11

- Локальные `core-api` и `streamlit-ui` штатно остановлены. Порты 8000 и 8501 свободны.
- Временные сессии и их контекст удалены при остановке API; это ожидаемое поведение прототипа.
- Код, правила, настройки, тесты, документация и снимки UI подготовлены к фиксации
  локальным Git-коммитом. Отправка в удалённый репозиторий не выполняется.
- Секреты для прототипа не требуются. `.env.example` содержит только безопасные настройки;
  локальные окружения Python и реальные файлы секретов в Git не включаются.
- Для продолжения активировать `source .venv/runtime-env/bin/activate` (Python 3.14.7)
  и выполнить команды запуска из `README.md`. После запуска будут созданы новые пустые сессии.
- Концепт и снимки интерфейса сохранены в `docs/ui-concept.md` и `docs/chat-*.png`.

## Ближайшие задачи

- Проверить сборку и запуск Docker Compose на машине с Docker.
- Согласовать следующий шаг: реальная классификация и обработка одной из тем.
